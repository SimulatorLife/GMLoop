{
  "name": "@gmloop/core",
  "version": "0.0.1",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "resource-directory.json"
  ],
  "dependencies": {
    "@bscotch/yy": "^2.7.0"
  },
  "devDependencies": {
    "typescript": "catalog:"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "build:types": "pnpm run generate:resource-directory && tsc -p tsconfig.json",
    "clean:types": "rm -rf dist",
    "test": "pnpm run build:types && node --test-force-exit --test dist/test/*.test.js",
    "generate:resource-directory": "node --input-type=module -e \"import { writeFileSync } from 'node:fs'; import { resolve } from 'node:path'; const workspaceDirectory = process.cwd(); const resourceDirectory = resolve(workspaceDirectory, '../../resources'); writeFileSync(resolve(workspaceDirectory, 'resource-directory.json'), JSON.stringify({ resourceDirectory }, null, 2) + '\\n');\""
  }
}
