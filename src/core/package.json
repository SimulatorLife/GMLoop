{
  "name": "@gmloop/core",
  "version": "0.0.1",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./package.json": "./package.json"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@bscotch/yy": "^2.7.0"
  },
  "devDependencies": {
    "typescript": "catalog:"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "build:types": "tsc -p tsconfig.json",
    "clean:types": "rm -rf dist",
    "test": "pnpm run build:types && node --test-force-exit --test dist/test/*.test.js"
  }
}
