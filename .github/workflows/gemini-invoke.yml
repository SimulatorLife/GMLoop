name: '▶️ Gemini Invoke'

on:
  issue_comment:
    types: [created]

permissions:
  contents: write
  pull-requests: write
  issues: write

concurrency:
  group: '${{ github.workflow }}-invoke-${{ github.event_name }}-${{ github.event.issue.number }}'
  cancel-in-progress: false

defaults:
  run:
    shell: 'bash'

jobs:
  invoke:
    if: ${{ github.event.issue.pull_request && contains(github.event.comment.body || '', '@gemini') }}
    uses: ./.github/workflows/agent-invoke.yml
    secrets:
      api_key: ${{ secrets.GEMINI_API_KEY }}
      GH_USER_TOKEN: ${{ secrets.GH_USER_TOKEN }}
    with:
      pr_number: ${{ github.event.issue.number }}
      additional_context: ${{ github.event.comment.body }}
      agent: gemini
      agent_package: ${{ vars.GEMINI_CLI_PACKAGE || '@google/gemini-cli@latest' }}
      validate_local_endpoint: false
      agent_setup_command: |
        set -euo pipefail

        if [ -z "${API_KEY:-}" ]; then
          echo "❌ Missing GEMINI_API_KEY secret (mapped to API_KEY)."
          exit 1
        fi

        if ! command -v gemini >/dev/null 2>&1; then
          echo "❌ Gemini CLI is not available after install step."
          exit 1
        fi

        export GEMINI_API_KEY="${API_KEY}"

        selected_model="auto"
        if [ -f .gemini/settings.json ] && command -v jq >/dev/null 2>&1; then
          selected_model="$({
            jq -er '.model.name | select(type == "string" and length > 0)' .gemini/settings.json
          } 2>/dev/null || echo "auto")"
        fi

        export GEMINI_MODEL="${selected_model}"
        echo "GEMINI_MODEL=${GEMINI_MODEL}" >> "$GITHUB_ENV"
        echo "Using Gemini model: ${selected_model}"
      agent_command: |
        set -euo pipefail

        if ! command -v gemini >/dev/null 2>&1; then
          echo "❌ Gemini CLI is not installed."
          exit 1
        fi

        export GEMINI_CLI_TRUST_WORKSPACE=true
        export GEMINI_API_KEY="${API_KEY}"

        if [ -z "${AGENT_PROMPT:-}" ]; then
          echo "❌ Missing shared AGENT_PROMPT from parent workflow."
          exit 1
        fi

        set +e
        stdbuf -oL -eL gemini \
          --approval-mode=yolo \
          --policy=.gemini/policy \
          --skip-trust \
          --model "${GEMINI_MODEL}" \
          --prompt "${AGENT_PROMPT}" 2>&1 | tee "$RUNNER_TEMP/gemini.log"
        gemini_status="${PIPESTATUS[0]}"
        set -e

        if [ "${gemini_status}" -ne 0 ]; then
          exit "${gemini_status}"
        fi
